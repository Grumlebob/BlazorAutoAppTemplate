# Cloud Caddy

Cloud Caddy configuration lives here.

Option A uses Cloudflare Tunnel. Caddy should bind locally on `cloud-main` and reverse proxy to `cloud-app1` and `cloud-app2` over the Hetzner private network.
