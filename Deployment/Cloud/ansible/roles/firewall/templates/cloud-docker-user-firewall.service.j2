[Unit]
Description={{ app_name }} Cloud Docker published-port firewall
After=docker.service
Wants=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/{{ app_name }}-cloud-docker-user-firewall
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
