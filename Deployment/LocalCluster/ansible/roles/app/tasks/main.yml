---
- name: Require app_version
  ansible.builtin.assert:
    that:
      - app_version is defined
      - app_version | length > 0
    fail_msg: "Pass -e app_version=<git-sha> when deploying app servers."

- name: Ensure deployment root exists
  ansible.builtin.file:
    path: "{{ deploy_root }}"
    state: directory
    owner: deploy
    group: deploy
    mode: "0750"

- name: Copy app compose file
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/../../compose/app-server/docker-compose.yml"
    dest: "{{ deploy_root }}/docker-compose.yml"
    owner: deploy
    group: deploy
    mode: "0640"

- name: Ensure app observability Docker network exists
  ansible.builtin.command: "docker network create {{ observability_docker_network }}"
  register: app_observability_network_create
  failed_when:
    - app_observability_network_create.rc != 0
    - "'already exists' not in app_observability_network_create.stderr"
  changed_when: app_observability_network_create.rc == 0

- name: Render app environment file
  ansible.builtin.template:
    src: app.env.j2
    dest: "{{ deploy_root }}/.env"
    owner: deploy
    group: deploy
    mode: "0640"

- name: Log in to GHCR
  ansible.builtin.shell: |
    set -eu
    echo "{{ vault_ghcr_token }}" | docker login ghcr.io -u "{{ vault_ghcr_username }}" --password-stdin
  args:
    executable: /bin/bash
  become: true
  become_user: deploy
  register: app_ghcr_login
  changed_when: false
  no_log: true

- name: Pull and start app container
  ansible.builtin.command: docker compose up -d --pull always
  args:
    chdir: "{{ deploy_root }}"
  become: true
  become_user: deploy
  register: app_compose_up
  retries: 5
  delay: 15
  until: app_compose_up.rc == 0
  changed_when: >-
    (app_compose_up.stdout ~ app_compose_up.stderr)
    is search("(?i)(created|started|recreated|pulled|downloaded|built|removed)")

- name: Wait for app health endpoint
  ansible.builtin.uri:
    url: "http://127.0.0.1:{{ app_port }}/health/ready"
    status_code: 200
  register: app_health
  retries: 30
  delay: 3
  until: app_health.status == 200
  changed_when: false
