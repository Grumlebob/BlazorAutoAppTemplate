global:
  resolve_timeout: 5m

route:
  receiver: local-observability
  group_by:
    - alertname
    - deployment_target
    - job
    - instance
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-observability
